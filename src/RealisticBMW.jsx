import React, { useEffect, useRef, useState } from 'react';
import * as THREE from 'three';
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js';
import { MeshoptDecoder } from 'three/examples/jsm/libs/meshopt_decoder.module.js';
import { Rotate3D, ZoomIn } from 'lucide-react';
import './realistic-bmw.css';

const MODEL_URL = 'https://raw.githubusercontent.com/lukaizj/car-mod-saas/main/public/models/bmw-m4.web.glb';
const VIEWS = [['FRONT','ENGINE & DIAGNOSTICS','Front-end inspection and precision scanning.'],['RIGHT','SUSPENSION & WHEELS','Steering, suspension and wheel safety check.'],['REAR','EXHAUST & PERFORMANCE','Exhaust, catalytic converter and road performance.'],['LEFT','BRAKES & BODY','Brake, tyre and exterior condition inspection.']];
function normalizeAngle(value){const two=Math.PI*2;let a=value%two;if(a<0)a+=two;return a;}
function styleBMWMaterial(material){if(!material)return;const name=String(material.name||'').toLowerCase();const isGlass=/glass|window|windshield|windscreen|transparent/.test(name);const isTyre=/tire|tyre|rubber|wheel|rim/.test(name);const isLight=/head|lamp|light|led|indicator|turn/.test(name);const isChrome=/chrome|metal|grille|trim/.test(name);if(isLight){if('color'in material)material.color.setHex(0x8f0710);if('emissive'in material)material.emissive.setHex(0xff0714);if('emissiveIntensity'in material)material.emissiveIntensity=.72;if('roughness'in material)material.roughness=.16;return;}if(isGlass){if('color'in material)material.color.setHex(0x030405);if('roughness'in material)material.roughness=.1;if('metalness'in material)material.metalness=.3;if('transparent'in material)material.transparent=true;if('opacity'in material)material.opacity=.84;return;}if(isTyre){if('color'in material)material.color.setHex(0x0b0c0c);if('roughness'in material)material.roughness=.58;return;}if(isChrome){if('color'in material)material.color.setHex(0x363a39);if('metalness'in material)material.metalness=.86;if('roughness'in material)material.roughness=.2;return;}if('color'in material)material.color.setHex(0x050607);if('metalness'in material)material.metalness=.76;if('roughness'in material)material.roughness=.17;}
export default function RealisticBMW(){const mountRef=useRef(null);const frameRef=useRef(0);const stateRef=useRef({angle:0,targetAngle:0,zoom:1,targetZoom:1,drag:false,x:0});const[activeView,setActiveView]=useState(0);const[loading,setLoading]=useState(true);const[failed,setFailed]=useState(false);useEffect(()=>{const mount=mountRef.current;if(!mount)return undefined;const scene=new THREE.Scene();const mobile=window.innerWidth<=700;const camera=new THREE.OrthographicCamera(-1,1,1,-1,.1,100);camera.position.set(0,.35,10);camera.lookAt(0,.05,0);const renderer=new THREE.WebGLRenderer({alpha:true,antialias:!mobile,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,mobile?1.15:1.6));renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.02;renderer.shadowMap.enabled=!mobile;renderer.shadowMap.type=THREE.PCFSoftShadowMap;mount.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xdde9e2,0x030807,1.5));const key=new THREE.DirectionalLight(0xfff4df,2.15);key.position.set(4,5,7);key.castShadow=!mobile;scene.add(key);const rim=new THREE.DirectionalLight(0x68d99c,1.15);rim.position.set(-5,3,-5);scene.add(rim);const redGlow=new THREE.PointLight(0xff0612,2.8,7);redGlow.position.set(0,.55,4.2);scene.add(redGlow);
const group=new THREE.Group();
// Keep the BMW higher in the hero frame while preserving one stable rotation pivot.
group.position.set(0,2.15,0);
scene.add(group);
const loader=new GLTFLoader();loader.setMeshoptDecoder(MeshoptDecoder);loader.load(MODEL_URL,gltf=>{const model=gltf.scene;model.traverse(obj=>{if(!obj.isMesh)return;obj.castShadow=!mobile;obj.receiveShadow=!mobile;if(Array.isArray(obj.material))obj.material.forEach(styleBMWMaterial);else styleBMWMaterial(obj.material);});const box=new THREE.Box3().setFromObject(model);const center=box.getCenter(new THREE.Vector3());const size=box.getSize(new THREE.Vector3());model.position.set(-center.x,-center.y,-center.z);model.scale.setScalar(3.58/Math.max(size.x,size.y,size.z));group.add(model);setLoading(false);},undefined,()=>{setLoading(false);setFailed(true);});const resize=()=>{const w=Math.max(mount.clientWidth,1),h=Math.max(mount.clientHeight,1),aspect=w/h,halfHeight=mobile?3.05:2.9;camera.left=-halfHeight*aspect;camera.right=halfHeight*aspect;camera.top=halfHeight;camera.bottom=-halfHeight;camera.updateProjectionMatrix();renderer.setSize(w,h,false);};resize();const ro=new ResizeObserver(resize);ro.observe(mount);const animate=()=>{const s=stateRef.current;s.angle+=(s.targetAngle-s.angle)*.12;s.zoom+=(s.targetZoom-s.zoom)*.12;group.rotation.y=s.angle;camera.zoom=s.zoom;camera.updateProjectionMatrix();renderer.render(scene,camera);frameRef.current=requestAnimationFrame(animate);};animate();const onPointerDown=e=>{stateRef.current.drag=true;stateRef.current.x=e.clientX;mount.setPointerCapture?.(e.pointerId);};const onPointerMove=e=>{const s=stateRef.current;if(!s.drag)return;s.targetAngle+=(e.clientX-s.x)*.012;s.x=e.clientX;};const onPointerUp=()=>{stateRef.current.drag=false;};const onWheel=e=>{e.preventDefault();stateRef.current.targetZoom=THREE.MathUtils.clamp(stateRef.current.targetZoom-e.deltaY*.0007,.82,1.28);};const onTouchMove=e=>{if(stateRef.current.drag&&e.touches.length===1)e.preventDefault();};mount.addEventListener('pointerdown',onPointerDown);mount.addEventListener('pointermove',onPointerMove);mount.addEventListener('pointerup',onPointerUp);mount.addEventListener('pointercancel',onPointerUp);mount.addEventListener('wheel',onWheel,{passive:false});mount.addEventListener('touchmove',onTouchMove,{passive:false});const viewTimer=window.setInterval(()=>{const angle=normalizeAngle(stateRef.current.angle);setActiveView(Math.round(angle/(Math.PI/2))%4);},80);return()=>{window.clearInterval(viewTimer);cancelAnimationFrame(frameRef.current);ro.disconnect();mount.removeEventListener('pointerdown',onPointerDown);mount.removeEventListener('pointermove',onPointerMove);mount.removeEventListener('pointerup',onPointerUp);mount.removeEventListener('pointercancel',onPointerUp);mount.removeEventListener('wheel',onWheel);mount.removeEventListener('touchmove',onTouchMove);renderer.dispose();renderer.forceContextLoss?.();if(renderer.domElement.parentNode===mount)mount.removeChild(renderer.domElement);};},[]);return <div className="bmw-experience" aria-label="Interactive BMW M4 360 degree showroom"><div ref={mountRef} className="bmw-canvas" />{!loading&&!failed&&VIEWS.map((item,index)=>{const distance=Math.min(Math.abs(index-activeView),4-Math.abs(index-activeView)),isActive=distance===0;return <div key={item[0]} className={`bmw-hotspot hotspot-${index} ${isActive?'is-active':''}`} aria-hidden={!isActive}><span>{item[0]} VIEW</span><strong>{item[1]}</strong><small>{item[2]}</small></div>;})}{loading&&<div className="bmw-loading">LOADING 3D VEHICLE<span>Preparing the showroom</span></div>}{failed&&<div className="bmw-loading">3D VIEW UNAVAILABLE<span>Please refresh to retry</span></div>}<div className="bmw-controls"><span><Rotate3D/> DRAG TO ROTATE 360°</span><span><ZoomIn/> SCROLL TO ZOOM</span></div></div>;}
